# BuriedTown Mod 项目蓝图

更新时间：2026-08-14

## 一句话愿景

在不推翻旧版 Cocos2d-JS 游戏运行基础的前提下，持续扩展角色、物品、建筑、
NPC 与生存机制，并逐步把散落在 UI 和旧核心文件中的业务规则收口到配置、
服务和明确的运行时边界中。

## 核心定位

- 本项目是 BuriedTown/死亡日记的长期 Mod 工程，不是一次性改包脚本。
- 当前阶段是“内容扩展 + 渐进式结构收口”，不是整体重写。
- `assets/src/jsList.js` 仍是脚本装配真源，旧资源体系和存档兼容必须继续工作。
- 新能力优先落在配置表、现有 Service/Manager/Router/State 和共享 UI 基础层。

## 目标形态

### 当前

- 已建立内容配置、运行时服务、校验工具和 smoke 护栏。
- runtime、购买链、shop state、部分 restore/migration 与特殊物品规则已完成收口。
- 仍保留旧核心文件、旧 Cocos UI 和资源 fallback，以兼容现有游戏行为。
- 当前工作树存在一批未提交的建筑、电力、角色运行时、时间和 UI 改动，必须作为
  活跃开发内容保护，不能按清理项处理。

### 近期

- 完成当前未提交功能的验证与工程收尾。
- 推进 R7：梳理 `uiUtil.js`、`dialog.js` 的职责，逐步把业务编排下沉到服务层。
- 保持内容扩展闭环：配置、逻辑、UI、资源、文案和校验必须同时完成。
- 继续补齐启动、读档、建筑动作、NPC 访问和电力网络等运行时 smoke。

### 理想

- 内容主数据集中在 `assets/src/data`。
- 业务规则集中在 `assets/src/game` 的服务和状态层。
- UI 只负责展示、交互和调用明确的业务接口。
- `GameRuntime` 是唯一运行时入口，启动、存档恢复和迁移边界清晰。
- 新增角色、物品、建筑和机制时，不需要在多个旧核心文件中重复加入硬编码分支。

## 架构边界

| 层 | 主要职责 | 代表路径 |
|---|---|---|
| 内容主数据 | 角色、物品、建筑、动作、公式和文案 | `assets/src/data/` |
| 规则与服务 | 解释配置、执行业务规则、提供稳定接口 | `assets/src/game/*Service.js`、`*Manager.js` |
| 状态模型 | 玩家、建筑、站点、动作和持久化状态 | `player.js`、`Build.js`、`site.js` |
| UI 与装配 | 场景、节点、弹窗、脚本加载和兼容入口 | `assets/src/ui/`、`assets/src/jsList.js` |
| 验证工具 | 内容校验、运行时 smoke、资源检查 | `tools/` |

## 明确不做

- 不整体重写为新引擎或新框架。
- 不随意调整 `assets/src/jsList.js` 的加载顺序。
- 不为了单一页面再建立一套平行业务状态或临时 helper。
- 不用大量 fallback、短路判断和兼容分支掩盖职责混乱。
- 不在没有最小回归的情况下大规模改动 UI、战斗、存档或购买链。
- 不把原版资源、破解手记和提取资产当作普通缓存删除。

## 质量门槛

每次功能或结构改动至少满足：

1. 说明配置、业务、UI、资源和存档影响范围。
2. 对改动过的 JavaScript 执行语法检查。
3. 按改动范围运行 `tools/validate-content.js` 定向校验。
4. 高风险入口运行 `tools/run-smoke.js runtime-boundaries startup` 或对应 smoke。
5. UI 改动完成资源与最小场景检查。
6. 在 `log.md` 记录实际结果、验证命令和遗留风险。

## 当前技术债

- `uiUtil.js` 与 `dialog.js` 仍承载部分业务编排，R7 尚未完成。
- 角色默认建筑等业务默认值仍混在 fallback 配置中，未来需要独立 schema 设计。
- 旧核心和新服务并存，任何收口都必须维持启动与存档兼容。
- 当前工作树有尚未提交的跨配置、运行时、建筑和 UI 改动，需要先验证和归档。

## 文档职责

- `AGENTS.md`：当前入口、协作规则、目录和风险清单。
- `SPEC.md`：长期定位、目标架构、非目标和质量门槛。
- `PLAN.md`：阶段、批次、整改主题和实施路线。
- `log.md`：已经发生的工程变更、验证结果和决策记录。
- `CLAUDE.md`：特定工具的使用和操作约定。
